# Server-side Next.js rewrite for auth, Gmail, cases and downloads.
BACKEND_URL=http://127.0.0.1:8000

# Public backend origin for long-running uploads and case extraction.
# Set both values to the Render HTTPS origin in Vercel production.
NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000
